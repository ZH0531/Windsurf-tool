# 🚀 Windsurf Account Switcher

跨平台的 Windsurf 账号切换工具

## 🎯 技术方案

### Electron + Node.js

- ✅ **跨平台**：Windows / macOS / Linux
- ✅ **原生能力**：可以使用 `safeStorage` 解密
- ✅ **图形界面**：用户友好
- ✅ **独立运行**：不依赖 Windsurf

### 架构设计

```
windsurf-switcher/
├── package.json              # 项目配置
├── main.js                   # Electron 主进程
├── preload.js                # 预加载脚本
├── renderer/                 # 渲染进程（UI）
│   ├── index.html
│   ├── style.css
│   └── renderer.js
└── modules/                  # 核心模块
    ├── deviceManager.js      # 设备码管理
    ├── sessionManager.js     # Session 管理
    ├── apiClient.js          # Windsurf API
    └── dbManager.js          # 数据库操作
```

## 🔑 核心功能

1. **读取当前账号** - 从 state.vscdb 读取并解密
2. **切换账号** - 从 refresh_token 自动获取新账号
3. **重置设备码** - 生成新的设备标识
4. **备份恢复** - 自动备份，支持恢复
5. **账号管理** - 保存多个账号，快速切换

## 🌟 优势

### vs 插件方式
- ✅ 不会与官方插件冲突
- ✅ 可以完全控制加密解密
- ✅ 可以直接操作文件和数据库

### vs Python 脚本
- ✅ 有图形界面，更友好
- ✅ 跨平台更容易
- ✅ 可以打包成单个可执行文件

## 📦 打包方案

使用 `electron-builder` 打包：

- **Windows**: `.exe` 安装包
- **macOS**: `.dmg` 或 `.app`
- **Linux**: `.AppImage` 或 `.deb`

## 🚀 快速开始

```bash
cd windsurf-switcher
npm install
npm start          # 开发模式
npm run build      # 打包
```
